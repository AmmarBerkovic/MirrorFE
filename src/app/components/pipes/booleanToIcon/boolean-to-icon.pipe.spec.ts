import { BooleanToIconPipe } from './boolean-to-icon.pipe';

describe('BooleanToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
